# Lead Generation System - Virtual Host Configuration

server {
    listen 80;
    listen [::]:80;
    server_name leadgen.local;
    
    # Redirect HTTP to HTTPS (for production)
    # return 301 https://$server_name$request_uri;
    
    # Root directory for static files
    root /usr/share/nginx/html;
    index index.html;
    
    # Access logs
    access_log /var/log/nginx/leadgen_access.log main;
    error_log /var/log/nginx/leadgen_error.log warn;
    
    # Health check endpoint (no rate limiting)
    location /health {
        proxy_pass http://leadgen_api/health;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        access_log off;
    }
    
    # Ready check endpoint
    location /ready {
        proxy_pass http://leadgen_api/ready;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        access_log off;
    }
    
    # API endpoints
    location /api/ {
        # Rate limiting
        limit_req zone=api_limit burst=200 nodelay;
        limit_conn conn_limit 100;
        
        # Proxy to upstream
        proxy_pass http://leadgen_api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Request-ID $request_id;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 120s;
        
        # Buffering
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
        proxy_busy_buffers_size 8k;
    }
    
    # Swagger UI
    location /docs {
        proxy_pass http://leadgen_api/docs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # ReDoc
    location /redoc {
        proxy_pass http://leadgen_api/redoc;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # OpenAPI JSON
    location /openapi.json {
        proxy_pass http://leadgen_api/openapi.json;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    # Metrics endpoint (internal only)
    location /metrics {
        allow 127.0.0.1;
        allow 10.0.0.0/8;
        deny all;
        
        proxy_pass http://leadgen_api/metrics;
        proxy_set_header Host $host;
    }
    
    # Static files
    location /static/ {
        alias /usr/share/nginx/html/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Favicon
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    
    # Robots.txt
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }
    
    # Error pages
    error_page 404 /404.html;
    location = /404.html {
        root /usr/share/nginx/html;
        internal;
    }
    
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
        internal;
    }
}


# HTTPS Configuration (for production)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name leadgen.example.com;
#     
#     ssl_certificate /etc/letsencrypt/live/leadgen.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/leadgen.example.com/privkey.pem;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:50m;
#     ssl_session_tickets off;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers off;
#     
#     add_header Strict-Transport-Security "max-age=63072000" always;
#     
#     # Same location blocks as above...
# }